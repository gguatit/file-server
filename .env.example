# 파일 서버 환경 변수 (Cloudflare Workers)

# Cloudflare Workers에서는 아래 변수를 wrangler secret으로 설정하세요.
# API_KY: Bearer 토큰 인증에 사용할 비밀키
#   npx wrangler secret put API_KEY

# wrangler.toml에 이미 설정된 변수들:
# - MAX_UPLOAD_SIZE: 262144000 (250MB)
# - RATE_LIMIT_PER_MINUTE: 60
# - ALLOWED_ORIGIN: https://kalpha.mmv.kr
